export interface SatRange {
  satTotalRange: {
    min: number;
    max: number;
  };
  satMathRange: {
    min: number;
    max: number;
  };
  satReadingRange: {
    min: number;
    max: number;
  };
}
