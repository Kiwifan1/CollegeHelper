export interface SatRange {
  satTotalRange: {
    min: number | null;
    max: number | null;
  };
  satMath: {
    min: number | null;
    max: number | null;
  };
  satReading: {
    min: number | null;
    max: number | null;
  };
}
